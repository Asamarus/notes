import { createStyles } from '@mantine/core';

export default createStyles((theme) => ({
  loader: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    padding: '200px 0',
  },
}));
